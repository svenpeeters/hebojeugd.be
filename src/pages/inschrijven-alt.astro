---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Lid worden | VC HEBO Jeugd">
  

    <!-- Photo Strip -->
    <div class="photo-strip">
      <div class="photo-strip__track" id="photoStrip"></div>
    </div>
    <script>
      const allPhotos = Array.from({length: 59}, (_, i) => `/images/paastoernooi-2025/${String(i + 1).padStart(2, '0')}.jpg`);
      const shuffled = allPhotos.sort(() => Math.random() - 0.5).slice(0, 25);
      const doubled = [...shuffled, ...shuffled];
      const track = document.getElementById('photoStrip');
      if (track) {
        track.innerHTML = doubled.map(src => `<img src="${src}" alt="" />`).join('');
      }
    </script>

    <!-- Section 1: Giant Question -->
    <section class="section section--question">
      <div class="giant-text">
        <span class="giant-text__line">KLAAR</span>
        <span class="giant-text__line">OM TE</span>
        <span class="giant-text__line giant-text__line--accent">SCOREN?</span>
      </div>
      <div class="scroll-hint">
        <span>Scroll</span>
        <div class="scroll-hint__arrow">↓</div>
      </div>
    </section>

    <!-- Section 2: Age + Price Combined -->
    <section class="section section--select">
      <div class="select-container">
        <h2 class="select-title">Kies je team</h2>
        
        <div class="age-grid">
          <button class="age-card" data-price="75" data-price-new="75">
            <span class="age-card__age">3-5</span>
            <span class="age-card__label">Kleuters</span>
            <span class="age-card__teams">U5</span>
            <div class="age-card__price">
              <span class="age-card__price-label">Lidgeld</span>
              <span class="age-card__price-amount">€75</span>
            </div>
          </button>
          <button class="age-card" data-price="250" data-price-new="275">
            <span class="age-card__age">5-9</span>
            <span class="age-card__label">Onderbouw</span>
            <span class="age-card__teams">U6 U7 U8 U9</span>
            <div class="age-card__price">
              <span class="age-card__price-label">Lidgeld</span>
              <span class="age-card__price-amount">€250</span>
            </div>
          </button>
          <button class="age-card" data-price="250" data-price-new="275">
            <span class="age-card__age">9-13</span>
            <span class="age-card__label">Middenbouw</span>
            <span class="age-card__teams">U10 U11 U12 U13</span>
            <div class="age-card__price">
              <span class="age-card__price-label">Lidgeld</span>
              <span class="age-card__price-amount">€250</span>
            </div>
          </button>
          <button class="age-card" data-price="275" data-price-new="300">
            <span class="age-card__age">13-17</span>
            <span class="age-card__label">Bovenbouw</span>
            <span class="age-card__teams">U14 U15 U17</span>
            <div class="age-card__price">
              <span class="age-card__price-label">Lidgeld</span>
              <span class="age-card__price-amount">€275</span>
            </div>
          </button>
        </div>

        <div class="discount-badge">
          <span>🎉</span>
          <span>2e kind? -€25 korting!</span>
        </div>
      </div>
    </section>

    <!-- Section 3: What You Get -->
    <section class="section section--stuff">
      <div class="stuff-container">
        <div class="stuff-header">
          <h2 class="stuff-title">Volledig uitgerust<br/>vanaf dag één</h2>
          <p class="stuff-subtitle">Elk lid krijgt een compleet kledijpakket. Alles wat je nodig hebt om er als een echte HEBO-speler uit te zien.</p>
        </div>
        
        <div class="stuff-grid">
          <div class="stuff-item" style="--delay: 0">
            <span class="stuff-item__icon">👕</span>
            <span class="stuff-item__name">Wedstrijd-shirt</span>
          </div>
          <div class="stuff-item" style="--delay: 1">
            <span class="stuff-item__icon">🩳</span>
            <span class="stuff-item__name">Wedstrijd-short</span>
          </div>
          <div class="stuff-item" style="--delay: 2">
            <span class="stuff-item__icon">👔</span>
            <span class="stuff-item__name">Wedstrijd-polo</span>
          </div>
          <div class="stuff-item" style="--delay: 3">
            <span class="stuff-item__icon">🧦</span>
            <span class="stuff-item__name">2× Kousen</span>
          </div>
          <div class="stuff-item" style="--delay: 4">
            <span class="stuff-item__icon">🦺</span>
            <span class="stuff-item__name">Trainingsvest</span>
          </div>
          <div class="stuff-item" style="--delay: 5">
            <span class="stuff-item__icon">👖</span>
            <span class="stuff-item__name">Trainingsbroek</span>
          </div>
          <div class="stuff-item" style="--delay: 6">
            <span class="stuff-item__icon">🧥</span>
            <span class="stuff-item__name">Regenjas</span>
          </div>
          <div class="stuff-item" style="--delay: 7">
            <span class="stuff-item__icon">⚽</span>
            <span class="stuff-item__name">Voetbal</span>
          </div>
          <div class="stuff-item" style="--delay: 8">
            <span class="stuff-item__icon">🥤</span>
            <span class="stuff-item__name">Drinkbus</span>
          </div>
        </div>

        <div class="stuff-bonus">
          <span class="stuff-bonus__badge">NIEUW LID?</span>
          <span class="stuff-bonus__text">Je krijgt er een voetbaltas bij! 🎒</span>
        </div>
      </div>
    </section>

    <!-- Section 4: Checklist -->
    <section class="section section--checklist">
      <div class="checklist-container">
        <h2 class="checklist-title">Ben je er klaar voor?</h2>
        
        <div class="checklist-items">
          <label class="check-item">
            <input type="checkbox" />
            <span class="check-item__box"></span>
            <span class="check-item__text">Voetbalschoenen</span>
          </label>
          <label class="check-item check-item--required">
            <input type="checkbox" />
            <span class="check-item__box"></span>
            <span class="check-item__text">Scheenbeschermers</span>
            <span class="check-item__badge">Verplicht!</span>
          </label>
          <label class="check-item">
            <input type="checkbox" />
            <span class="check-item__box"></span>
            <span class="check-item__text">Goesting om te leren</span>
          </label>
          <label class="check-item">
            <input type="checkbox" />
            <span class="check-item__box"></span>
            <span class="check-item__text">Zin in plezier</span>
          </label>
        </div>
        
        <div class="checklist-note">
          <span>💡</span>
          <span>Indoorschoenen nodig voor indoortrainingen</span>
        </div>
      </div>
    </section>

    <!-- Section 5: Final CTA -->
    <section class="section section--cta">
      <div class="cta-container">
        <div class="cta-text">
          <span class="cta-line">DOE</span>
          <span class="cta-line cta-line--accent">MEE</span>
        </div>
        <p class="cta-subtitle">Word lid van VC HEBO Jeugd</p>
        <a href="mailto:info@hebojeugd.be" class="cta-button">
          <span>Contact opnemen</span>
          <span class="cta-arrow">→</span>
        </a>
      </div>
    </section>

  
</BaseLayout>

<script>
  // Intersection Observer for animations
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('in-view');
      }
    });
  }, { threshold: 0.2 });

  document.querySelectorAll('.section').forEach(section => {
    observer.observe(section);
  });

  // Age card selection
  document.querySelectorAll('.age-card').forEach(card => {
    card.addEventListener('click', () => {
      document.querySelectorAll('.age-card').forEach(c => c.classList.remove('age-card--selected'));
      card.classList.add('age-card--selected');
    });
  });

  // Checklist interactivity
  document.querySelectorAll('.check-item input').forEach(input => {
    input.addEventListener('change', () => {
      const allChecked = [...document.querySelectorAll('.check-item input')].every(i => i.checked);
      if (allChecked) {
        document.querySelector('.section--cta')?.classList.add('all-checked');
      }
    });
  });
</script>

<style>
  main {
    background: #0a0f1a;
    color: #fff;
  }

  /* Photo Strip */
  .photo-strip {
    overflow: hidden;
    background: #1e3264;
    padding: 0.75rem 0;
  }

  .photo-strip__track {
    display: flex;
    gap: 1rem;
    animation: stripScroll 20s linear infinite;
  }

  @keyframes stripScroll {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }

  :global(.photo-strip__track img) {
    width: 160px;
    height: 110px;
    object-fit: cover;
    border-radius: 6px;
    flex-shrink: 0;
  }

  .section {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 4rem 2rem;
    position: relative;
    opacity: 0.3;
    transform: translateY(50px);
    transition: all 0.8s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .section.in-view {
    opacity: 1;
    transform: translateY(0);
  }

  /* Section 1: Question */
  .section--question {
    background: linear-gradient(135deg, #c8202f 0%, #7f1d1d 100%);
    opacity: 1;
    transform: none;
  }

  .giant-text {
    display: flex;
    flex-direction: column;
    text-align: center;
  }

  .giant-text__line {
    font-size: clamp(4rem, 18vw, 14rem);
    font-weight: 900;
    line-height: 0.85;
    letter-spacing: -0.02em;
  }

  .giant-text__line--accent {
    color: #fff;
    -webkit-text-stroke: 3px #fff;
    -webkit-text-fill-color: transparent;
  }

  .scroll-hint {
    position: absolute;
    bottom: 3rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.85rem;
    text-transform: uppercase;
    letter-spacing: 3px;
    opacity: 0.7;
    animation: bounce 2s infinite;
  }

  @keyframes bounce {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(10px); }
  }

  /* Section 2: Select */
  .section--select {
    background: #0a0f1a;
    padding: 6rem 2rem;
  }

  .select-container {
    max-width: 1000px;
    width: 100%;
    text-align: center;
  }

  .select-title {
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 900;
    margin-bottom: 3rem;
  }

  .age-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 1.5rem;
  }

  .age-card {
    background: rgba(255,255,255,0.05);
    border: 2px solid rgba(255,255,255,0.1);
    padding: 2rem;
    border-radius: 1.5rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    cursor: pointer;
    transition: all 0.3s ease;
    color: #fff;
    font-family: inherit;
  }

  .age-card:hover {
    background: rgba(255,255,255,0.1);
    border-color: rgba(255,255,255,0.2);
    transform: translateY(-5px);
  }

  .age-card--selected {
    border-color: #c8202f;
    background: rgba(200, 32, 47, 0.2);
  }

  .age-card__age {
    font-size: 3.5rem;
    font-weight: 900;
    line-height: 1;
  }

  .age-card__label {
    font-size: 1rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 2px;
    color: #c8202f;
  }

  .age-card__teams {
    font-size: 0.85rem;
    opacity: 0.5;
    letter-spacing: 1px;
  }

  .age-card__price {
    margin-top: 1rem;
    padding-top: 1rem;
    border-top: 1px solid rgba(255,255,255,0.1);
    width: 100%;
  }

  .age-card__price-label {
    display: block;
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 1px;
    opacity: 0.5;
    margin-bottom: 0.25rem;
  }

  .age-card__price-amount {
    font-size: 1.8rem;
    font-weight: 900;
  }

  .discount-badge {
    margin-top: 2rem;
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    background: #c8202f;
    padding: 1rem 2rem;
    border-radius: 100px;
    font-weight: 700;
  }

  /* Section 3: Stuff */
  .section--stuff {
    background: #1e3264;
    padding: 6rem 2rem;
  }

  .stuff-container {
    max-width: 900px;
    width: 100%;
  }

  .stuff-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .stuff-title {
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 900;
    line-height: 1.1;
    margin-bottom: 1rem;
  }

  .stuff-subtitle {
    font-size: 1.1rem;
    opacity: 0.7;
    max-width: 500px;
    margin: 0 auto;
    line-height: 1.6;
  }

  .stuff-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: 1rem;
  }

  .stuff-item {
    background: rgba(255,255,255,0.1);
    padding: 1.5rem;
    border-radius: 1rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    text-align: center;
    opacity: 0;
    transform: translateY(20px);
    transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1);
    transition-delay: calc(var(--delay) * 0.08s);
  }

  .section--stuff.in-view .stuff-item {
    opacity: 1;
    transform: translateY(0);
  }

  .stuff-item__icon {
    font-size: 2rem;
  }

  .stuff-item__name {
    font-size: 0.9rem;
    font-weight: 600;
  }

  .stuff-bonus {
    margin-top: 3rem;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
  }

  .stuff-bonus__badge {
    background: #c8202f;
    padding: 0.5rem 1.5rem;
    border-radius: 100px;
    font-size: 0.8rem;
    font-weight: 800;
    letter-spacing: 1px;
  }

  .stuff-bonus__text {
    font-size: 1.3rem;
    font-weight: 700;
  }

  /* Section 4: Checklist */
  .section--checklist {
    background: #fff;
    color: #0a0f1a;
    padding: 6rem 2rem;
  }

  .checklist-container {
    max-width: 600px;
    width: 100%;
  }

  .checklist-title {
    font-size: clamp(2rem, 5vw, 3rem);
    font-weight: 900;
    margin-bottom: 2rem;
    text-align: center;
    color: #1e3264;
  }

  .checklist-items {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .check-item {
    display: flex;
    align-items: center;
    gap: 1rem;
    cursor: pointer;
    padding: 1.25rem 1.5rem;
    background: #f5f5f5;
    border-radius: 1rem;
    transition: all 0.2s ease;
  }

  .check-item:hover {
    background: #eee;
  }

  .check-item input {
    display: none;
  }

  .check-item__box {
    width: 1.75rem;
    height: 1.75rem;
    border: 3px solid #1e3264;
    border-radius: 0.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.2s ease;
    flex-shrink: 0;
  }

  .check-item input:checked + .check-item__box {
    background: #4ade80;
    border-color: #4ade80;
  }

  .check-item input:checked + .check-item__box::after {
    content: '✓';
    color: #fff;
    font-weight: 700;
  }

  .check-item__text {
    font-size: 1.2rem;
    font-weight: 700;
    flex: 1;
  }

  .check-item--required {
    background: #fef2f2;
  }

  .check-item--required .check-item__text {
    color: #c8202f;
  }

  .check-item__badge {
    background: #c8202f;
    color: #fff;
    padding: 0.25rem 0.75rem;
    border-radius: 100px;
    font-size: 0.75rem;
    font-weight: 700;
  }

  .checklist-note {
    margin-top: 2rem;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    font-size: 0.9rem;
    color: #666;
  }

  /* Section 5: CTA */
  .section--cta {
    background: linear-gradient(135deg, #1e3264 0%, #0a0f1a 100%);
    min-height: 80vh;
    padding: 6rem 2rem;
  }

  .cta-container {
    text-align: center;
  }

  .cta-text {
    display: flex;
    flex-direction: column;
  }

  .cta-line {
    font-size: clamp(5rem, 22vw, 16rem);
    font-weight: 900;
    line-height: 0.85;
  }

  .cta-line--accent {
    color: #c8202f;
  }

  .cta-subtitle {
    font-size: 1.2rem;
    opacity: 0.7;
    margin: 1.5rem 0 2.5rem;
  }

  .cta-button {
    display: inline-flex;
    align-items: center;
    gap: 1rem;
    padding: 1.25rem 2.5rem;
    background: #c8202f;
    color: #fff;
    text-decoration: none;
    font-size: 1.1rem;
    font-weight: 700;
    border-radius: 100px;
    transition: all 0.3s ease;
  }

  .cta-button:hover {
    background: #fff;
    color: #c8202f;
    transform: scale(1.05);
  }

  .cta-arrow {
    font-size: 1.3rem;
    transition: transform 0.3s ease;
  }

  .cta-button:hover .cta-arrow {
    transform: translateX(5px);
  }

  .section--cta.all-checked .cta-button {
    animation: pulse 1s infinite;
  }

  @keyframes pulse {
    0%, 100% { box-shadow: 0 0 0 0 rgba(200, 32, 47, 0.5); }
    50% { box-shadow: 0 0 0 20px rgba(200, 32, 47, 0); }
  }

  /* Responsive */
  @media (max-width: 600px) {
    .section {
      padding: 4rem 1.5rem;
    }

    .age-grid {
      grid-template-columns: 1fr 1fr;
      gap: 1rem;
    }

    .age-card {
      padding: 1.5rem 1rem;
    }

    .age-card__age {
      font-size: 2.5rem;
    }

    .stuff-grid {
      grid-template-columns: repeat(3, 1fr);
      gap: 0.75rem;
    }

    .stuff-item {
      padding: 1rem;
    }

    .stuff-item__icon {
      font-size: 1.5rem;
    }

    .stuff-item__name {
      font-size: 0.75rem;
    }

    :global(.photo-strip__track img) {
      width: 120px;
      height: 85px;
    }
  }
</style>
