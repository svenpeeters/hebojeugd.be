---
import Layout from '../layouts/Layout.astro';
---

<Layout title="VC HEBO Jeugd">
  <main>
    <!-- Hero Section -->
    <section class="hero">
      <div class="hero-content">
        <div class="hero-eyebrow">Est. 2024 — Heers × Eendracht Mechelen-Bovelingen</div>
        <h1 class="hero-title">
          <span class="line">We don't just</span>
          <span class="line accent">play football.</span>
          <span class="line">We raise</span>
          <span class="line accent">future legends.</span>
        </h1>
      </div>
      <div class="hero-visual">
        <div class="logo-container">
          <img src="/logo.png" alt="HEBO Jeugd" class="floating-logo" />
        </div>
        <div class="hero-stats">
          <div class="stat-item">
            <span class="stat-value">8</span>
            <span class="stat-label">Teams</span>
          </div>
          <div class="stat-item">
            <span class="stat-value">200+</span>
            <span class="stat-label">Spelers</span>
          </div>
          <div class="stat-item">
            <span class="stat-value">2</span>
            <span class="stat-label">Terreinen</span>
          </div>
        </div>
      </div>
      <div class="scroll-indicator">
        <span>Scroll</span>
        <div class="scroll-line"></div>
      </div>
    </section>

    <!-- Manifesto Section -->
    <section class="manifesto">
      <div class="manifesto-content">
        <p class="manifesto-text">
          <span class="highlight">Vergeet alles</span> wat je weet over jeugdvoetbal. 
          Geen ouders die schreeuwen langs de lijn. 
          Geen trainers die winnen belangrijker vinden dan groeien.
          <span class="highlight">Hier telt maar één ding:</span> 
          plezier in het spel.
        </p>
      </div>
    </section>

    <!-- Values Section -->
    <section class="values">
      <div class="value-card">
        <span class="value-number">01</span>
        <h3 class="value-title">Respect</h3>
        <p class="value-desc">Voor jezelf, je team, je tegenstander.</p>
      </div>
      <div class="value-card featured">
        <span class="value-number">02</span>
        <h3 class="value-title">Teamgeest</h3>
        <p class="value-desc">Alleen ga je sneller. Samen kom je verder.</p>
      </div>
      <div class="value-card">
        <span class="value-number">03</span>
        <h3 class="value-title">Plezier</h3>
        <p class="value-desc">Want waarom spelen we anders?</p>
      </div>
    </section>

    <!-- Locations Section -->
    <section class="locations">
      <h2 class="section-title">Waar vind je ons?</h2>
      <div class="locations-grid">
        <a href="https://maps.google.com/?q=Raes+van+Heerslaan+Heers" target="_blank" class="location-card">
          <div class="location-name">Heers VV</div>
          <div class="location-address">Raes van Heerslaan, 3870 Heers</div>
          <div class="location-arrow">↗</div>
        </a>
        <a href="https://maps.google.com/?q=Gelindenstraat+Mechelen-Bovelingen" target="_blank" class="location-card">
          <div class="location-name">EMBO</div>
          <div class="location-address">Gelindenstraat z/n, 3870 Mechelen-Bovelingen</div>
          <div class="location-arrow">↗</div>
        </a>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="cta">
      <div class="cta-content">
        <h2 class="cta-title">Klaar om<br/>mee te doen?</h2>
        <a href="https://www.voetbalvlaanderen.be/club/9732/komende-wedstrijden" target="_blank" class="cta-button">
          <span>Bekijk wedstrijden</span>
          <span class="btn-arrow">→</span>
        </a>
      </div>
    </section>

    <!-- Footer -->
    <footer class="footer">
      <div class="footer-grid">
        <div class="footer-col footer-brand-col">
          <img src="/logo.png" alt="HEBO Jeugd" class="footer-logo" />
          <p class="footer-manifesto">
            Geboren uit fusie.<br/>
            <span>Gedreven door passie.</span>
          </p>
        </div>
        
        <div class="footer-col">
          <h4>Navigate</h4>
          <a href="#" class="footer-link-big">Home</a>
          <a href="#" class="footer-link-big">Teams</a>
          <a href="#" class="footer-link-big">Contact</a>
        </div>
        
        <div class="footer-col">
          <h4>Extern</h4>
          <a href="https://www.voetbalvlaanderen.be/club/9732" target="_blank" class="footer-link-big">Voetbal Vlaanderen ↗</a>
          <a href="https://www.voetbalvlaanderen.be/club/9732/komende-wedstrijden" target="_blank" class="footer-link-big">Wedstrijden ↗</a>
        </div>
        
        <div class="footer-col">
          <h4>Sociaal</h4>
          <a href="#" class="footer-link-big">Facebook</a>
          <a href="#" class="footer-link-big">Instagram</a>
        </div>
      </div>
      
      <div class="footer-bottom">
        <div class="footer-locations">
          <span>Heers VV</span>
          <span class="dot">•</span>
          <span>EMBO</span>
        </div>
        <div class="footer-stamp">
          STAMNR 09827
        </div>
        <div class="footer-copy">
          © 2026
        </div>
      </div>
      
      <div class="footer-marquee">
        <div class="marquee-content">
          <span>HEBO JEUGD</span>
          <span>•</span>
          <span>RESPECT</span>
          <span>•</span>
          <span>TEAMGEEST</span>
          <span>•</span>
          <span>PLEZIER</span>
          <span>•</span>
          <span>HEBO JEUGD</span>
          <span>•</span>
          <span>RESPECT</span>
          <span>•</span>
          <span>TEAMGEEST</span>
          <span>•</span>
          <span>PLEZIER</span>
          <span>•</span>
        </div>
      </div>
    </footer>
  </main>
</Layout>

<style>
  /* Reset & Base */
  main {
    background: #fafafa;
    color: #1a1a1a;
    overflow-x: hidden;
  }

  /* Hero */
  .hero {
    min-height: 100vh;
    display: grid;
    grid-template-columns: 1fr 1fr;
    position: relative;
    background: linear-gradient(135deg, #0a0f1a 0%, #1e3264 100%);
    color: #fff;
  }

  .hero-content {
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 4rem;
    padding-left: 8vw;
  }

  .hero-eyebrow {
    font-size: 0.85rem;
    letter-spacing: 3px;
    text-transform: uppercase;
    opacity: 0.6;
    margin-bottom: 2rem;
  }

  .hero-title {
    font-size: clamp(2.5rem, 5vw, 4.5rem);
    font-weight: 800;
    line-height: 1.1;
    margin: 0;
  }

  .hero-title .line {
    display: block;
  }

  .hero-title .accent {
    color: #c8202f;
    font-style: italic;
  }

  .hero-visual {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    position: relative;
  }

  .logo-container {
    position: relative;
  }

  .floating-logo {
    width: 300px;
    animation: float 6s ease-in-out infinite;
    filter: drop-shadow(0 30px 60px rgba(0,0,0,0.4));
  }

  @keyframes float {
    0%, 100% { transform: translateY(0) rotate(0deg); }
    50% { transform: translateY(-20px) rotate(3deg); }
  }

  .hero-stats {
    display: flex;
    gap: 3rem;
    margin-top: 4rem;
  }

  .stat-item {
    text-align: center;
  }

  .stat-value {
    display: block;
    font-size: 3rem;
    font-weight: 800;
    line-height: 1;
  }

  .stat-label {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 2px;
    opacity: 0.6;
  }

  .scroll-indicator {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
  }

  .scroll-indicator span {
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 2px;
    opacity: 0.5;
  }

  .scroll-line {
    width: 1px;
    height: 40px;
    background: linear-gradient(to bottom, #fff, transparent);
    animation: scrollPulse 2s ease-in-out infinite;
  }

  @keyframes scrollPulse {
    0%, 100% { opacity: 0.3; height: 40px; }
    50% { opacity: 1; height: 60px; }
  }

  /* Manifesto */
  .manifesto {
    padding: 10rem 8vw;
    background: #fafafa;
  }

  .manifesto-text {
    font-size: clamp(1.5rem, 3vw, 2.5rem);
    line-height: 1.6;
    max-width: 900px;
    margin: 0;
    color: #333;
  }

  .manifesto-text .highlight {
    color: #c8202f;
    font-weight: 700;
  }

  /* Values */
  .values {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    min-height: 60vh;
  }

  .value-card {
    padding: 4rem;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    background: #fff;
    border-right: 1px solid #eee;
    transition: background 0.4s ease;
  }

  .value-card:last-child {
    border-right: none;
  }

  .value-card:hover {
    background: #f5f5f5;
  }

  .value-card.featured {
    background: #1e3264;
    color: #fff;
  }

  .value-card.featured:hover {
    background: #2a4080;
  }

  .value-number {
    font-size: 0.75rem;
    letter-spacing: 2px;
    opacity: 0.4;
    margin-bottom: auto;
  }

  .value-title {
    font-size: 2.5rem;
    font-weight: 800;
    margin: 0 0 1rem 0;
  }

  .value-desc {
    font-size: 1rem;
    opacity: 0.7;
    margin: 0;
  }

  /* Locations */
  .locations {
    padding: 8rem 8vw;
    background: #1a1a1a;
    color: #fff;
  }

  .section-title {
    font-size: 0.85rem;
    text-transform: uppercase;
    letter-spacing: 3px;
    opacity: 0.5;
    margin-bottom: 3rem;
  }

  .locations-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
  }

  .location-card {
    padding: 3rem;
    border: 1px solid #333;
    text-decoration: none;
    color: #fff;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    transition: all 0.3s ease;
    position: relative;
  }

  .location-card:hover {
    background: #fff;
    color: #1a1a1a;
    border-color: #fff;
  }

  .location-name {
    font-size: 2rem;
    font-weight: 800;
  }

  .location-address {
    opacity: 0.6;
    font-size: 0.95rem;
  }

  .location-arrow {
    position: absolute;
    top: 2rem;
    right: 2rem;
    font-size: 1.5rem;
    opacity: 0;
    transform: translate(-10px, 10px);
    transition: all 0.3s ease;
  }

  .location-card:hover .location-arrow {
    opacity: 1;
    transform: translate(0, 0);
  }

  /* CTA */
  .cta {
    padding: 10rem 8vw;
    background: #c8202f;
    color: #fff;
    display: flex;
    justify-content: space-between;
    align-items: center;
    position: relative;
    overflow: hidden;
  }

  .cta-title {
    font-size: clamp(3rem, 8vw, 7rem);
    font-weight: 800;
    line-height: 1;
    margin: 0 0 2rem 0;
  }

  .cta-button {
    display: inline-flex;
    align-items: center;
    gap: 1rem;
    background: #fff;
    color: #c8202f;
    padding: 1.2rem 2.5rem;
    text-decoration: none;
    font-weight: 700;
    font-size: 1rem;
    transition: all 0.3s ease;
  }

  .cta-button:hover {
    background: #1a1a1a;
    color: #fff;
    gap: 1.5rem;
  }

  .btn-arrow {
    transition: transform 0.3s ease;
  }

  .cta-button:hover .btn-arrow {
    transform: translateX(5px);
  }

  .cta-stamp {
    display: flex;
    flex-direction: column;
    align-items: center;
    opacity: 0.3;
    transform: rotate(12deg);
  }

  .cta-stamp span:first-child {
    font-size: 0.7rem;
    letter-spacing: 3px;
  }

  .stamp-number {
    font-size: 4rem;
    font-weight: 900;
    line-height: 1;
  }

  /* Footer */
  .footer {
    background: #0a0f1a;
    color: #fff;
    padding: 8rem 8vw 0;
  }

  .footer-grid {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr 1fr;
    gap: 4rem;
    padding-bottom: 6rem;
    border-bottom: 1px solid #222;
  }

  .footer-col h4 {
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 3px;
    opacity: 0.4;
    margin-bottom: 2rem;
  }

  .footer-brand-col {
    padding-right: 4rem;
  }

  .footer-logo {
    width: 25%;
    max-width: 250px;
    margin-bottom: 2rem;
  }

  .footer-manifesto {
    font-size: 1.5rem;
    line-height: 1.4;
    opacity: 0.6;
    margin: 0;
  }

  .footer-manifesto span {
    color: #c8202f;
    opacity: 1;
    font-weight: 700;
  }

  .footer-link-big {
    display: block;
    color: #fff;
    text-decoration: none;
    font-size: 1.3rem;
    font-weight: 600;
    margin-bottom: 1rem;
    transition: all 0.2s ease;
  }

  .footer-link-big:hover {
    color: #c8202f;
    transform: translateX(10px);
  }

  .footer-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 3rem 0;
  }

  .footer-locations {
    display: flex;
    gap: 1rem;
    font-size: 0.85rem;
    opacity: 0.5;
  }

  .footer-locations .dot {
    opacity: 0.3;
  }

  .footer-stamp {
    font-size: 0.75rem;
    letter-spacing: 4px;
    opacity: 0.3;
  }

  .footer-copy {
    font-size: 0.85rem;
    opacity: 0.3;
  }

  .footer-marquee {
    background: #c8202f;
    margin: 0 -8vw;
    padding: 1.5rem 0;
    overflow: hidden;
  }

  .marquee-content {
    display: flex;
    gap: 2rem;
    animation: marquee 20s linear infinite;
    white-space: nowrap;
  }

  .marquee-content span {
    font-size: 1rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 3px;
  }

  @keyframes marquee {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }

  /* Responsive */
  @media (max-width: 900px) {
    .hero {
      grid-template-columns: 1fr;
      min-height: auto;
    }

    .hero-content {
      padding: 6rem 2rem 3rem;
      text-align: center;
    }

    .hero-visual {
      padding: 3rem 2rem 6rem;
    }

    .floating-logo {
      width: 200px;
    }

    .hero-stats {
      gap: 2rem;
    }

    .stat-value {
      font-size: 2rem;
    }

    .manifesto {
      padding: 5rem 2rem;
    }

    .values {
      grid-template-columns: 1fr;
    }

    .value-card {
      padding: 3rem 2rem;
      border-right: none;
      border-bottom: 1px solid #eee;
    }

    .locations {
      padding: 4rem 2rem;
    }

    .locations-grid {
      grid-template-columns: 1fr;
    }

    .cta {
      padding: 5rem 2rem;
      flex-direction: column;
      text-align: center;
      gap: 3rem;
    }

    .cta-stamp {
      transform: rotate(0deg);
    }

    .footer {
      padding: 4rem 2rem 0;
    }

    .footer-grid {
      grid-template-columns: 1fr;
      gap: 3rem;
      text-align: center;
    }

    .footer-brand-col {
      padding-right: 0;
    }

    .footer-link-big {
      font-size: 1.1rem;
    }

    .footer-link-big:hover {
      transform: none;
    }

    .footer-bottom {
      flex-direction: column;
      gap: 1rem;
      text-align: center;
    }

    .footer-marquee {
      margin: 0 -2rem;
    }
  }
</style>
